# Local Data

Place local metadata and preprocessed NCCT volumes here if convenient. CSV, NIfTI, NPY, DICOM, and archive files are ignored by git.

Recommended local layout:

```text
data/
├── meta_with_phase_center_stratified_balanced.csv
└── center_data/
    └── center/manufacturer/model/kernel/*.npy
```
